

{# <a class="p-2 text-dark" href="{{path('admin')}}">My account</a> #}

{% for category in categories %}
    <a class="p-2 text-dark" href="{{path('videoList', {'categoryname':category.name, 'id': category.id})}}">{{category.name}}</a>
{% endfor %}